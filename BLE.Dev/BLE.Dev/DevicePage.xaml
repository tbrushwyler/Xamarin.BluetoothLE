<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BLE.Dev.DevicePage">
  <ContentPage.Content>

	<StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
		<Label Text="{Binding AdvertiseStatus}"></Label>
      <ListView x:Name="ListView" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" ItemsSource="{Binding Devices}" BackgroundColor="Maroon">
        <ListView.ItemTemplate>
          <DataTemplate>
            <TextCell Text="{Binding Name}" Detail="{Binding ServiceCount}"></TextCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      </StackLayout>
  </ContentPage.Content>
</ContentPage>